                                                           Assignment 1
 1. Write a program that takes an integer from user then print it
 Example
 Input 5
 Output 5
                                                          // Answer

#include <iostream>;
using namespace std;
int main() {
	int num{};
	cout << "Enter Number" << endl;
	cin >> num;
	cout << "Your Number is = " << num;
}

//========================================================================================================================================
 2. write a program that ask the user to insert 3 numbers then print the sum
 Input
Please enter first number:
 5
 Please enter second number
 3
 Please enter third number
 4
 Output
 Summation of 3 integers is 12
                                                       // Answer


#include <iostream>;
using namespace std;
int main() {
	int num1{}, num2{}, num3{};
	cout << "Please enter first number:" << endl;
	cin >> num1;
	cout << "Please enter seconder:" << endl;
	cin >> num2;
	cout << "Please enter third  number:" << endl;
	cin >> num3;
	cout << " Summation of 3 integers is " << num1 + num2 + num3;
}
//==========================================================================================================================================
 3. write a program that ask the user to insert 3 integers then print the average as
 decimal number
 Input:
 Please enter first number:
 6
 Please enter second number
 4
 Please enter third number
 4
 Output:
 The average = 4.66667
                                                           // Answer
#include <iostream>;
using namespace std;
int main() {
	int num1{}, num2{}, num3{};
float average;

	cout << "Please enter first number:" << endl;
	cin >> num1;
	cout << "Please enter second number:" << endl;
	cin >> num2;
	cout << "Please enter third  number:" << endl;
	cin >> num3;
	average = (num1 + num2 + num3) / 3.0;
	cout << " The Average is " << average;
}
//==========================================================================================================================================
4. Write a program to compute the perimeter and area of a rectangle. Your code
 should ask the user to insert the width and the height
 Input(example)
 Width = 5
 Height = 3
 Output
 The perimeter = 16
 The area = 15
                                                             // Answer
#include <iostream>;
using namespace std;
int main() {
	int width, height, perimeter, area;
	cout << "please Enter the width and height of rectangle" << endl;
	cin >> width >> height;
	perimeter =( width + height) * 2;
	area = width * height;
	cout << "The perimeter = " << perimeter << endl << "The area = " << area ;

}
//========================================================================================================================================
 5. Write a program to calculate the sum of the digits of a 3-digit number.
 Input :  132
 Output : 6
                                            // Answer 
#include <iostream>;
using namespace std;
int main() {
	int num{}, sum;
	cout << "Enter A number" << endl;
	cin >> num;
	sum = num % 10;
	num = num / 10;
	sum += num % 10;
	num = num / 10;
	sum += num % 10;
	cout << sum;

}
//========================================================================================================================================
6. Write a program to convert a given integer (in seconds) to hours,
 minutes and seconds.
 Input seconds: 25300
 Expected Output: H:M:S - 7:1:40
                                                                             // Answer
#include <iostream>;
using namespace std;
int main() {
	int total_seconds, remaining_seconds, hours, minutes, seconds;
	cout << "Enter Seconds\n";
	cin >> total_seconds;
	hours = total_seconds / 3600;
	 remaining_seconds = total_seconds % 3600;
	minutes = remaining_seconds / 60;
	seconds = remaining_seconds % 60;
	cout << "H:M:S = " << hours << " : " << minutes << " : " << seconds;
}



//==========================================================================================================================================
 7. Write a program that allows the user to insert an integer then print negative if it
 is a negative number otherwise print positive.
 Example
 Input -5
 Output negative
 Example2
 Input 10
 Output positive
                                                              // Answer
#include <iostream>;
using namespace std;
int main() {
	int num{};
	cout << "Entger A Number " << endl;
	cin >> num;
	if (num > 0) {
		cout << "This Is Positive\n" ;
	}
	else if (num < 0) {
		cout << "This Is Negative\n";
	}
	else
	{
		cout << "Is Equal To Zero\n";
	}
}
//=====================================================================================================================================================
 8. Write a program to read any digit from 1 to 5, display it as a word
 ex : input: 4, output: four
                                                                   // Answer
#include <iostream>;
using namespace std;
int main() {
	int num{};
	cout << "Entger A Number From 1 to 5" << endl;
	cin >> num;
	if (num == 1) {
		cout << "One\n" ;
	}
	else if (num == 2) {
		cout << "Two\n";
	}
	else if (num == 3) {
		cout << "Three\n";
	}
	else if (num == 4) {
		cout << "Four\n";
	}
	else if (num == 5) {
		cout << "Five\n";
	}
	else
	{
		cout << "Out Of Range\n";
	}
}
//========================================================================================================================================================
9. Write a program that reads 5 numbers and sum of all odd values between
 them.
 ● Test Data :11  17 13 12 5
 ● Expected Output:  Sum of all odd values: 46


                                                                              // Answer
#include <iostream>;
using namespace std;
int main() {
	int num1{}, num2{}, num3{}, num4{}, num5{}, sum{};
	cout << "Entger A 5 Numbers" << endl;
	cin >> num1 >> num2 >> num3 >> num4 >> num5;

	if (num1 % 2 != 0) {
		sum += num1;
	}
 if (num2 % 2 != 0) {
		sum += num2;
}
if (num3 % 2 != 0) {
		sum += num3;
}
	 if (num4 % 2 != 0) {
		sum += num4;
}
	 if (num5 % 2 != 0) {
		sum += num5;
}
	cout << sum;
}
//==============================================================================================================================================================
 10.Write a program that reads a year then check if a year is a leap year or not: a
 leap year is divisible by 400 or 4 only
                                                              // Answer
#include <iostream>;
using namespace std;
int main() {
	int year = 0;
	cout << " Enter The Year\n";
	cin >> year;
	if (year % 400 == 0 || year % 4 == 0) {
		cout << "This Is Leap Year\n";
	}
	else
	{
		cout << "Not Leap Year\n";
	}
} 
//=================================================================================================================================================================
 11. take the birth year and check if he is above 18 or not
                                                    // Answer
#include <iostream>;
using namespace std;
int main() {
	int birthYear, currentYear = 0;
	cout << " Enter The currentYear\n";
	cin >> currentYear;
	cout << "Enter Your Birth Year\n";
	cin >> birthYear;
	if (currentYear - birthYear > 18) {
		cout << "You Above 18\n";
	}
	else {
		cout << "You Less 18\n";
	}
}
//==========================================================================================================================================================================
12.A school has following rules for grading system:
 a. Below 25 - F
 b. 25 to 45 - E
 c. 45 to 50 - D
 d. 50 to 60 - C
 e. 60 to 80 - B
 f. Above 80 - A
 Ask the user to enter marks and print the corresponding grade. 
                                                              // Answer
#include <iostream>;
using namespace std;
int main() {
	int grade = 0;
	cout << "Enter Your Grade\n";
	cin >> grade;
	if (grade > 80) {
		cout << "A\n";
	}
	else if (grade <= 80 && grade >= 60) {
		cout << "B\n";
	}
	else if (grade < 60 && grade >= 50) {
		cout << "C\n";
	}
	else if (grade < 50 && grade >= 45) {
		cout << "D\n";
	}
	else if (grade < 45 && grade >= 25) {
		cout << "E\n";
	}
	else {
		cout << "F\n";
	}

}
//==================================================================================================================================================================================
 13.Search for Switch Case and use it to write a program that reads an integer between 1 and 12 and print the month of the year in English.
 ● Test Data :
 Insert a number between 1 to 12 to get the month name: 8
 ● Expected Output: August
                                                            // Answer
#include <iostream>;
using namespace std;
int main() {
	int Month;
	cout << "Enter The Number Of Month\n";
	cin >> Month;
	switch (Month) {
	case 1:
		cout << "January\n";
		break;

	case 2 :
		cout << "February\n";
		break;
	case 3 :
		cout << "March\n";
			break;
	case 4 :
		cout << "April\n";
		break;
	case 5:
		cout << "May\n";
		break;
	case 6:
		cout << "June\n";
		break;
	case 7 :
		cout << "July\n";
		break;
	case 8:
		cout << "August\n";
		break;
	case 9 :
		cout << "September\n";
		break;
	case 10: 
		cout << "October\n";
		break;
	case 11 :
		cout << "November\n";
		break;
	case 12 :
		cout << "December\n";
		break;
	default :
		cout << "Out Of Range\n";
		break;

	}
}
//========================================================================================================================================================================
14- Search for ternary operator and use it to write a program that allows the
user to insert integer number then Even or Odd
 ● Example  Input 8  Output even
 ● Example 2 Input 7 Output odd
                                                                        // Answer
#include <iostream>;
using namespace std;
int main() {
	int Number = 0;
	cout << "Enter Number\n";
	cin >> Number;
	Number % 2 == 0 ? cout << "This Is Even Number" : cout << "This Is Odd";


}
//=====================================================================================================================================================================
15 - Write a program that take character from user then if it is vowel chars (a,e,I,o,u)
 then print vowel otherwise print consonant
 ● Example   Input o  Output vowel
 ● Example 2 Input b Output consonant
                                                             // Answer 
#include <iostream>;
using namespace std;
int main() {
	char c;
	cout << "Enter Char\n";
	cin >> c;
	if (c == 'a' || c == 'A' || c == 'e' || c == 'E'
		|| c == 'o' || c == 'O' || c == 'i'
		|| c == 'I' || c == 'u' || c == 'U') {
		cout << "vowel\n";
	}
	else {
		cout << "consonant\n";
	}


}
//==================================================================================================================================================================================
16- Write a program that take 3 integer from user then print the max element and
 the min element.
 ● Example
 Input 7 8 5
 Output
 max element = 8
 min element = 5
 ● Example2
 Input 3 6 9
 Output
 Max element = 9
 Min element = 3
                                                                                    // Answer 
#include <iostream>;
using namespace std;
int main() {
	int num1, num2, num3, max , min;
	cout << "enter three numbers\n";
	cin >> num1 >> num2 >> num3;
	if (num1 > num2 && num1 > num3 ) {
		max = num1;
	}
	else if (num2 > num1 && num2 > num3) {
		max = num2;

	}
	else {
		max = num3;
	}
	if (num1 < num2 && num1 < num3) {
		min = num1;

	}
	else if (num2 < num1 && num2 < num3) {
		min = num2;
	}
	else {
		min = num3;

	}
	cout << " Max Element = " << max << endl;
	cout << " Min Element = " << min << endl;


}


